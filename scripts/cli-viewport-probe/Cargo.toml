[package]
name = "cli-viewport-probe"
version = "0.1.0"
edition = "2024"
publish = false

# Standalone on purpose: excluded from the workspace so it never adds build time
# to the product. It exists to answer ONE question with the daemon's OWN parser.
[workspace]

[dependencies]
portable-pty = "0.9"
vt100 = "0.16.2"
serde_json = "1"
