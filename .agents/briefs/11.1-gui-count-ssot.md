# 11.1 [scan-truth] — THE GUI'S OWN COUNTS DO NOT MATCH THE VERBS' — ACK-111-GUICOUNT-7b2e

Owner-reported 2026-08-20 ~14:22 with a frame, after the previous 11.1 lane landed the
CLI-side fix: *"startpage, cwdtree (both gui and ls) are not still adhering to SSOT."*

## THE STATE, MEASURED BY 11.0 BEFORE SPAWNING YOU — do not re-derive, verify

Taken on the GUI host at 14:26, both verbs, no limit cap:

    server startpage ls --json --limit 100000   ->  durable_count = 792,  len(rows)    = 792
    server cwdtree  ls --json --limit 100000   ->  durable_count = 792,  group_count  = 90

⇒ **The two CLI verbs AGREE with each other and with their own payload lengths.** The previous
lane's identity fix holds; that is not what the owner is seeing.

From his frame, taken minutes earlier on the same host:

    start page, RECENT WORK header .......... "788 shown"
    sidebar machine nodes ................... dev 735 · oc 698 · practice 18 · jyas-webapp 2 · local 1
    sidebar Live Sessions ................... 47

⇒ **Two disagreements, and they are different in kind:**
1. **792 (verb) vs 788 (GUI start page)** on the same host, minutes apart. A four-row gap.
2. The sidebar's per-machine counts sum to ~1454 against a 792-row universe. Machine nodes are
   separate scan scopes, so this may be correct-by-construction rather than a bug — **establish
   which before touching anything.** Two of those machine keys are known to name one physical
   host, so a session can legitimately be reachable under two keys.

## YOUR JOB

Find the ONE owner of "how many durable sessions are there", and make every surface read it.
The verbs already agree; the GUI does not agree with the verbs. So the defect is now in the
GUI's own counting path, which is a different locality from the scan fix that already landed.

**Order of work:**
1. Settle disagreement (1) first — it is the smallest and the least ambiguous. Sample the verb
   and the GUI **at the same moment**; an instrument read taken at a different instant from the
   screen is not a verification of the screen, and that mistake has already produced one
   published-and-withdrawn finding on this campaign.
2. Then decide whether (2) is a bug at all. If per-machine counts are separate scopes by design,
   say so in the queue entry and close that half explicitly rather than leaving it ambiguous.
3. `Live Sessions 47` is a third count with a third owner. Check it against `live_count` from
   both verbs while you are in there.

## TRAPS THAT HAVE ALREADY COST THIS CAMPAIGN TIME

- ⛔ `--json` on these verbs silently caps at 200 without `--limit`. A count taken under the cap
  looks like a real number. Always pass `--limit`.
- ⛔ **`durable_count` was once computed BEFORE a dedup** and shipped beside a shorter row list in
  one object. When two counts disagree, ask whether one counts row INSTANCES and the other unique
  ENTITIES before calling it drift — that exact confusion produced a 700-vs-696 split on identical
  session sets.
- ⛔ A column that looks like a filter may filter nothing. Print a column's distribution BEFORE
  filtering on it; `killed=0` matched every one of 999 rows and eight sibling columns were
  uniformly empty too.
- ⛔ App control answers ONLY on the GUI host. Run it there; a headless host has a daemon and
  sessions but no client, and the error reads like an empty result.
- ⛔ The owner is AT THE MACHINE. His viewport is sacred: never activate a row, never type into a
  live prompt, and never run `input-check` (it WRITES a marker). `server gate-screen` is the read
  door. Spawn probe rows with `--no-activate`, and reap them when done.

## DELIVERABLE

One queue entry in `docs/pending-bugs.md` that names the single owner of the count and what each
surface does instead, then the fix, then a live comparison sampled simultaneously. Update
`docs/cli-integration.md` if the rule changes. Report to 11.0 over the peer plane when done.
Push every commit; `git fetch` before believing any behind/ahead count.

⛔ This repo is PUBLIC. No host names, no attributed speech, invent every example.
