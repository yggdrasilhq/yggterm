# Brief for the next 6.1 — the hot-restart relay gate

Seat `6.1`, host `dev`, cwd `/home/pi/gh/yggterm`. Orchestrator: row `6.0`.
Written 2026-08-13 ~19:00 IST by session `5493c67d`, at ~72% context.

## SECTION 0 — FIRST ACTS

1. `~/gh/yggterm/.agents/skills/yggterm-agent-fleet/ygg-claim.sh --host <gui-host> --title "yggterm: the hot-restart relay gate — the constitution's unmet guarantee" --number 6.1 --replace 5493c67d-f854-4453-b718-46aae61eb4aa --booter`
   ⚠ Auto-detect of the GUI host failed from `dev` again today; `--host` was required.
2. `ygg-monitor.py subscribe --uuid <yours> --role relay --seat 6.1 --machine dev --escalate-to <6.0 uuid> --intent "hot-restart relay gate"`
3. Read `docs/spec-hot-restart-relay-gate.md`, then the queue entry
   `## ⚖⚖ THE HOT-RESTART GATE IS UNBUILT` in `docs/pending-bugs.md`. The queue
   entry is current as of this brief and carries every falsifier.

## SECTION 1 — WHAT IS DONE

**§4 closed and live-proven** (3.0.124–3.0.127), by an earlier session. Do not rebuild.

**§2 and §5 are IN CODE AND PUSHED** — `91d69e7e` is the tip that carries them.
Both are **LIVE PROOF OWED**, and neither is deployed.

- §2: `server relay-boundary [--by] [--wait-secs] [--json]`; `ygg-claim.sh`
  declares one after a predecessor is retired **and reaped**. The boundary is a
  field on the §4 queue entry, released against BOTH the file floor and each
  drainer's process-local floor, and spent by the attempt it buys.
- §5: `hot_restart_deadline_verdict` (30 min, cold-shutdown gate only) plus
  `crates/yggterm-server/src/hot_restart_repair.rs` — the durable interrupted-set
  record and the `continue` injection that repairs it.
- Both surfaced on `server daemons`: an unspent boundary on the swap line, and a
  `repair owed:` line when one is outstanding.

**§3's remainder is deliberately NOT built**, and the queue entry says why at
length: the gate classifies from the live in-daemon vt100 screen and no shipped
verb exposes it, so a blocked-on-human recognizer has nothing to be validated
against. **The prerequisite is the smaller job** — expose the screen the GATE
reads, harvest real parked-at-a-question screens, then write the recognizer.

## SECTION 2 — YOUR SCOPE, IN ORDER

1. ⭐ **DEPLOY AND LIVE-PROVE §2 AND §5. This is the top of the list**, because
   two unproven mechanisms in the shipping path is exactly the state the campaign
   keeps having to dig itself out of. `scripts/bump-version.sh` (pushes the bump
   ALONE and first; refuses while you hold unpushed commits), build on `dev`,
   deploy per `deploy-via-hot-restart`. **Check the artifact's `--version` and
   mtime — `cargo build --release` has reported exit 0 while a binary failed to
   compile and the stale artifact stayed on disk.**
   - **§2 falsifier:** on a host owing a real swap with a daemon new enough to
     read the field, run `server relay-boundary`, then within ~20 s expect a
     handoff attempt whose queue `last_outcome` reads *"a relay boundary was
     declared; retrying the handoff at it"*.
   - **§5 falsifier, and BOTH halves must be checked:** a
     `hot_restart_forced_past_deadline` event naming its `interrupted` list,
     then one `hot_restart_repair_continue {outcome: "submitted"}` per session on
     the daemon that adopted them, with `server daemons` printing `repair owed:`
     in between. ⛔ A forced swap whose repair never lands is the deadline
     shipping alone, which is the thing the old prohibition forbade.
   - ⚠ The deadline cannot be manufactured to order (it needs 30 minutes of
     interruptible-only blockers). Catch it in the wild; do not fake it by
     shortening the constant on a live host.
2. **The gate-screen verb**, which unblocks §3's remainder. Read-only, on demand,
   and ⛔ **not into trace events** — session screen text in a persisted trace is
   a leak surface.
3. **Then** blocked-on-human, against the harvested corpus.
4. The positive WORKING signal (`session_transcript_activity`). The queue entry
   records that its SOURCE is proven and its TRANSPORT is not.

## SECTION 3 — WHAT WILL SAVE YOU TIME

- ⭐ **A bypass must release EVERY floor, or it releases none.** §2's boundary had
  two gates in series — the queue file's `attempt_is_due` and each drainer's
  process-local static. Releasing one would have printed success and changed
  nothing. Pinned source-level, because a missing condition on a second gate
  type-checks and passes every unit test.
- ⭐ **Spend an event on DISPATCH, never on success**, and derive "spent" rather
  than storing it. One boundary buys one attempt because
  `boundary_at_ms > last_attempt_ms`; one interruption buys one `continue`
  because the keys leave the record as the caller learns them. A second field
  recording spentness would be two encodings of one fact.
- ⛔ **A trailing flag reset never runs on a panic.** The repair lane's in-flight
  flag is now a `Drop` guard. Any `static BOOL` set true before work and false
  after it has this hole.
- ⚠ **`cargo test -p yggterm-server --lib` has a rare flake**:
  `tests::local_cc_relaunch_rebuild_collapses_poisoned_identity_to_row_id` failed
  1 run in ~10, passes in isolation and passed 8 consecutive full runs after.
  Order/parallelism dependent, pre-existing, not filed for lack of evidence. **If
  you see it, do not assume you broke something — reproduce it on a clean tree
  first.**
- ⚠ **The claim script is edited by peers in this shared checkout.** Read it
  fresh, edit narrowly, `bash -n` after.

## SECTION 4 — THE STATE YOU ARE BORN INTO

- **The push hold is LIFTED** (2026-08-13 ~18:50). Four force-pushes total, none
  further coming. Published tips: yggterm `dab86f6e` + everything after.
- ⛔ **Your local branch may be on a DEAD LINEAGE and the symptom is a guard that
  refuses everything.** After the rewrite, `origin/main` was current and `main`
  was not. `ygg-privacy-guard scan` then covers `<tip> --not --remotes=origin` =
  the whole history: it scanned **1,211,081 added lines and refused with 259
  hits**, all from history public for weeks, and the only lever its own text
  offers is `YGG_PRIVACY_ALLOW=1` — the override it says is how a real leak
  ships. After reconciling, the same scan reads **1,559 lines, clean.**
  ⇒ **Check `git rev-list --left-right --count origin/main...HEAD` before
  believing any guard refusal.** If the left number is in the thousands, the
  branch is the problem, not your commit.
  ⛔ **Reconcile by REPLAY, never `git rebase origin/main` and never a merge** —
  both reintroduce the pre-scrub history. `git rebase --onto origin/main <old
  base> main`.
- ⛔ **Never `git push --tags` / `--follow-tags`.** Every clone's tags pointed at
  the pre-relicensing lineage and were the only thing keeping ~1919 commits of
  pre-scrub history alive locally. A branch has to be named; tags go by accident.
- ⭐ **The law that produced today's near-miss, and it is structural:** *am I
  choosing between explanations, and does one of them happen to authorise what I
  was already about to do?* `origin/main` advancing during a declared hold has
  two readings — "the hold is off" and "an authorised party opened a window" —
  and only the second was true. Write the disjunction down and ask.
- ⭐ **Verify a peer's correction rather than adopting it.** 6.0 corrected my
  lineage inference and was right; verifying it independently turned up a fact
  their message did not have (`origin/main` had moved again since they wrote).
  Both directions of the row-to-row channel have been measured failing while
  reporting success, so **send, then read the RECEIVER's state** — grep their
  transcript for a marker from your own message.
- ⚠ **Row-to-row mail: `ListAgents` + `SendMessage` to the uds address**, never
  `terminal submit`. And the listing's name suffix does **not** encode the
  session uuid — I assumed it did, and only the receiver-side transcript grep
  proved delivery to the right session.
