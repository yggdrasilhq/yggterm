# Brief for the next 6.1 — the daemon drain and the booter lifecycle

Seat `6.1`, host `dev`, worktree `~/gh/yggterm--6.1`. Orchestrator: row `6.0`.
Rewritten 2026-08-14 ~02:5x UTC by session `970b54e9`, which shipped 3.0.154.

⛔⛔ **WORK IN `~/gh/yggterm--6.1`.** The shared checkout ate a session's
uncommitted work once; `git reset --hard` there took ~1200 lines.

## SECTION 0 — FIRST ACTS

1. `.agents/skills/yggterm-agent-fleet/ygg-claim.sh --host jojo --title "<what you are doing>" --number 6.1 --replace <predecessor uuid>`
   ⚠ `--host jojo` is the GUI host; your WORK is on dev. ⛔ **Resolve the
   predecessor uuid from `ls ~/.claude/projects/*/<prefix>*.jsonl`, never from a
   brief** — mine was handed a fabricated tail and the `--replace` was a silent
   no-op, leaving a dead row seated.
2. `ygg-monitor.py subscribe --uuid <yours> --machine dev --role relay --seat 6.1 --campaign yggterm --escalate-to <6.0's uuid> --intent "<your lane>"`
3. `python3 ~/.claude/hooks/context-relay-gauge.py --report` — early, and land at 70%.

## SECTION 0.5 — WHAT TO DO FIRST, GIVEN THE HOLD

**The drain's entire critical path is fixed and green. It is blocked on exactly
one thing: the 3.0.155 hold, which is OWNER-PROTECTIVE, not technical.** Do not
read the drain as blocked on engineering, and do not try to engineer around it.

**While the hold stands, in this order:**
1. ✅ **The UNKNOWN triage is DONE — `coverage` reports UNKNOWN 0** (2026-08-14,
   24 rows: 2 retired, 2 opted out, 1 subscribed, 19 never-arm). ⛔ Do not
   re-derive it; the ledgers in `~/.yggterm/relay/` carry the evidence per row.
   ⚠ **Two verbs now exist for the decisions coverage names** — `ygg-booter.py
   never-arm --row … --note …` and `optout --row … --note …`. Before them, the
   decision that mattered most was the only one with no instrument.
2. ⭐ **THE NEXT RELEASE IS THE EXPERIMENT — do not waste it.** The deaths are
   only observable at a deploy, and until now the evidence was deleted before
   anyone looked (§2). `deploy-fleet.sh` now pins the trace window
   automatically; after the bump, read the sweeps in the 0–600 s band.
3. Anything the orchestrator routes.

**The moment the hold lifts** (the owner's draft is sent or cleared): cut
3.0.155 carrying `lane/dev/6.6-arsenal` and 6.7's S2 daemon-side work, deploy,
then take the two proofs that need a predecessor at 3.0.154+ — the generator
signal (§1) and a lost-ack handover converging (§2).

## SECTION 1 — WHAT SHIPPED, AND THE ONE PROOF STILL OWED

**3.0.154 is deployed fleet-wide** (12 copies, dev/jojo/oc). It carries the
generator fix. Since then, on `main` and awaiting a release:

| commit | what |
|---|---|
| `544b11c0` | the retire lane remembers its own handoff instead of asking a host-shared file |
| `22f97c41` | a re-sent adoption of the SAME child is idempotent success, not a conflict |
| `d9966a7c` | the duplicate-prune never asks an owner to drop a runtime whose child we share |
| `de9cf741` | the handoff sweep attempts EVERY session instead of stopping at the first failure |
| `b4f46302` | a daemon that cannot read its client list now SAYS why it is refusing to retire |
| (this session) | an UNREADABLE client-instances dir is an error, not "no clients" — it used to PERMIT retiring out from under a live client |
| `9e94163c` `3c25bd61` `45bf37ef` `3d95929d` | booter: the opt-out ledger is read; `coverage`; `retire`; count sessions not rows |

⛔ **THE ONE THING YOU MUST NOT RE-DERIVE: `target_source:"process_memory"`
cannot appear until a 3.0.154-or-later daemon is a PREDECESSOR.** The generator
fix lives in the predecessor path. Until the next release supersedes the current
daemon, its positive signal is unobservable and its absence means nothing.

⚠ **The population watch is NOT the proof.** 16 daemons flat over an hour with
no deploy — but the pre-fix daemon in that same window was not generating either,
so the mechanism was never provoked. **A control that also passes supports
nothing.** That sentence caught three separate wrong conclusions in one session.

## SECTION 2 — THE DEATH IS EXPLAINED. READ THIS BEFORE PROPOSING ANY MECHANISM.

Sessions were dying minutes after each deploy and the cause was open all night.
For the 464 s instance it is now closed, end to end, in the trace:

1. handover starts; the successor **accepts the fd and seats it**, and the **ack
   is lost**;
2. the predecessor books a failure and **resumes its own readers** ⇒ the key is
   genuinely owned by BOTH daemons;
3. seven sweeps, one a minute, all `NoneMoved` — the successor refusing with
   *"already runs a live PTY for it"*, **which is proof the first attempt
   worked**, read as the reason it failed;
4. something calls `terminal_ensure_new`; the **duplicate-prune** fires; a drop
   is `remove_session` → `shutdown` → **`kill`**;
5. the child dies. Same second as the transcript's last write.

⭐ **The killer is neither the handover nor the idle gate. It is the
DEDUPLICATION of a duplicate the handover's own failure manufactured.** Both
landed fixes attack it: `22f97c41` stops the duplicate existing, `d9966a7c`
stops the prune killing a shared child.

⛔ **SCOPE, and do not widen it:** `removed_terminal:true` appears exactly twice
in the whole corpus. **The 272 s and 402 s deaths have none of them and remain
UNEXPLAINED.** Do not retro-fit this onto them.

### ⛔⛔ AND THE REASON THEY HAVE NONE IS RETENTION, NOT MECHANISM (2026-08-14)

**The corpus does not reach them.** Earliest record **01:05:11Z**; the four
deaths are 16:55Z, 19:50Z, 00:21Z, 00:22Z. Measured: 247.9 MiB against a 256 MiB
cap, written at **83.1 MiB/h** — the constant claimed *80 MiB/day* and promised
3 days, and the real window was **2.98 h**. The budget is per HOME while the rate
is per DAEMON, so **the window shrinks as the pile grows and is shortest during
the releases it exists to explain.**

⇒ **Withdraw every argument of the form "those deaths have no such events."** The
silence is structural. ✅ Cap now 1 GiB (~12 h); `scripts/pin-trace-window.sh`
hard-links the window and `deploy-fleet.sh` calls it, following 15 min.

### ⭐ A CANDIDATE WITH A TRACED TRIGGER PATH — the stale-daemon sweep

Not proven, no observed kill, and it does NOT retire the OOM rival. But it is the
first candidate that is not merely a timing coincidence:
`cleanup_legacy_linux_daemon_processes` runs **on every daemon start** and answers
with SIGTERM→SIGKILL 120 ms apart. ⛔ **"Legacy" is relative to the SWEEPER's own
binary path, not a version** — a build-tree daemon calls every installed daemon
legacy. Both gates discarded their errors, and **measured across 8 sweeps the
client gate protected NOBODY (`skipped_active_client_legacy` 0) while 16 daemons
were saved only by answering a status probe** — one unanswered probe from being
signalled, and a daemon serves one request at a time. ✅ Both now fail closed,
with `skipped_unreadable_clients` / `skipped_no_status` reported separately.
**The proof, at the next release:** `killed_legacy`/`killed_orphan` ≥ 1 in the
0–600 s band. Detail: `docs/pending-bugs.md` § *THE OTHER SEAT DEATHS*.

⛔ **Three mechanisms were proposed and refuted this session** — the 300 s idle
window (160 self-retire polls, **zero** idle-gate deferrals), the forced-past
-deadline path (0 events against a 175-hit positive control), and a suspected
trace/source contradiction (my own reader printing a missing key as `null`).
**Do not re-adopt any of them.**

## SECTION 3 — THE DRAIN: UNBLOCKED, NOT YET EXERCISED

### ⛔⛔ JUSTIFY S1 CORRECTLY OR IT WILL LOOK LIKE A FAILURE

**There is no leak.** A daemon whose sessions are QUIET costs ~0.67 ms per
request and its reader threads cost nearly nothing. The bursty cost is **one pty
reader thread per session, proportional to that session's OUTPUT VOLUME** — so
**the daemon population is expensive because it is BUSY, not because it is old
or numerous.** That is the second independent line ruling out a leak.

⇒ **The drain moves work onto fewer, CURRENT daemons. It does not remove work.**
Anyone selling S1 as *"reclaim leaked cores"* is promising a saving that will not
arrive, and its absence will read as the drain having failed. ⛔ **Do not send
anyone leak-hunting in the daemon.**

⭐ **The real reasons S1 is still right, and they are enough:** legacy daemons
lack every fix since their own version (and that grows with each release), they
carry frozen row inventories they do not own, and — the one this session
proved — **they are where the fatal handover paths live.**

⛔ **Do not quote any per-session core figure.** The sandbox number came from a
saturated pty (`yes(1)`), which no agent CLI approaches; live per-thread figures
are 0.02–0.19 cores. **The transferable claim is the SHAPE, never the magnitude.**

**Why the 14 legacy daemons never empty their hands:** one poisoned session pins
all of them. `classify_handoff_sweep` takes a `first_failure` and stops, so a
single stuck key held **eleven** healthy runtimes hostage (`readers_stood_down:
11, moved: 0`). Both halves are now fixed: `22f97c41` stops the key getting stuck that way, and
`de9cf741` stops one stuck key abandoning the rest — the loop attempts every
session and classifies at the end. ⚠ `Partial` is therefore much more common, and
`Partial` must NEVER exit the process; the code already knows that, but any
future edit there must keep knowing it.

⚠ **A "release" in `progressive_migration` KILLS the pty and re-resumes** — read
`release_session_for_migration`'s docstring before calling it a move. And it is
gated on being re-resumable, so **a plain shell can never be released that way**;
those daemons say so themselves (*"a plain shell with no store to resume from; a
cold shutdown would destroy it"*).

⇒ The drain cannot be exercised until 3.0.155 deploys. **Do not evict anything.**

### ⛔ S1's CLOSING CLAUSE — "then it retires on its own" — AND WHERE IT CAN FAIL

The drain is *move sessions off, then let the emptied daemon retire.* The second
half is a real dependency, and a sibling lane has one counter-example: a sandbox
daemon with the gates apparently open **lived 48 minutes against a 90 s window**.

`daemon_should_idle_shutdown` has **three gates**, and reading it explains that:

1. `terminal_session_count > 0` ⇒ never retire. ⚠ **This counts OWNED +
   PRESERVED**, so a daemon holding only bequeathed records still refuses.
2. not yet idle for `idle_shutdown_ms` ⇒ no.
3. `active_client_instance_records` empty ⇒ retire. **Otherwise it retires only
   if `daemon_is_superseded(home_dir)`.**

⇒ **Gate 3 is the one the drain rests on.** On dev every legacy daemon IS
superseded, so once its hands are empty it should go. **A sandbox typically has
no superseding daemon**, so gate 3 fails and an emptied daemon lingers for ever —
which would explain the 48-minute observation without any defect in the drain.

⚠ **Unverified — I read the source, I did not reproduce the sandbox.** Check
whether that sandbox had a strictly-newer daemon alive before treating it as a
counter-example to S1.

⭐ **AND THE FAIL-CLOSED EDGE WAS THE WRONG WAY ROUND — I HAD IT BACKWARDS.**
I first reported that an error keeps a daemon alive silently. The caller's error
arm exists, but **the callee could never reach it**: every failure ended in
`return Ok(())`. So the real defect was the OPPOSITE and far worse — an
unreadable client-instances directory (permissions, fd exhaustion, ENOMEM) read
as **no clients**, satisfied gate 3, and **PERMITTED a daemon to retire while a
client was still attached to it**. Two halves of one decision disagreed about
what unreadable means, and the careless half won. Caught by the resource lane.

✅ Fixed: **absent stays `Ok`** (a real answer — no directory, no records),
**unreadable travels as `Err`**, and so does an incomplete listing or a record
that cannot be read. That also makes `b4f46302`'s trace REACHABLE, which it was
not when I wrote it. ⚠ The two changes only make sense together.

## SECTION 4 — THE BOOTER: STEP 2 IS DEAD, AND WHY

⛔ **The agent-row filter the settled design asked for does not exist and cannot
be written.** Measured across all 31 fields of a 378-row listing: the owner's
attended row and a delegate are identical — same `kind`, same `icon_kind`, same
tenancy, same presence. The separators are free text, identity, a folding flag,
transient `busy`, and a seat number an agent types about itself.

⇒ `never-arm.tsv` is not a backstop beneath a filter, **it IS the filter**, so
enumerate-and-arm is **fail-open**. `never_arm()`'s own docstring already rules
that such a path does not ship. **Do not build it.**

What exists instead: `ygg-booter.py coverage` sorts every live row into
`watched / never-arm / opted-out / retired / UNKNOWN` and **refuses to act on
UNKNOWN**. `retire` records a decided-dead row with evidence. ⚠ Auto-arm stays
unbuilt on purpose: it would resurrect rows that unsubscribed by finishing.

✅ **UNKNOWN is 0** (2026-08-14). 24 rows decided: 2 retired, 2 opted out, 1
subscribed, 19 never-arm. Nineteen of them were the owner's own conversations —
told apart by their FIRST turn (a person's prose or an interactively typed slash
command) versus a spawn brief, which is the only separator that exists, and it is
not a row field.

⭐ **Two verbs now exist, because coverage named three decisions and the CLI had
instruments for two** — `never-arm --row … --note …` and `optout --row … --note
…`. Both read back and both drop a subscription they contradict.

⛔ **AND THE SCREEN ITSELF WAS FAIL-OPEN.** `never_arm()` returned `{}` for every
read error, so an unreadable attended-row list said *nobody is attended* to
callers whose next line arms a row — the weaker ledger beside it already failed
closed, so the two halves disagreed and the careless half owned the stronger
list. Now: absent stays `{}`, unreadable and torn travel as `None`, `subscribe`
/`retire`/`coverage` refuse, and the tick — the only thing that types — boots
nothing and says so every tick. The MONITOR's wake path screened nothing at all
(it sends a message and then a lone CR, which submits a person's draft); it now
screens, purges a subscription on an attended row, and wakes nobody when the list
cannot be read.

⚠ **Both watchers now run from `~/gh/yggterm`** (the shared checkout, ff'd to
main), not from a lane worktree — restarted 2026-08-14 09:10Z so the fixes are
live rather than on disk, verified by md5 of the file at each process's own cwd.
A lane worktree is a poor home for a fleet-wide watchdog; it can vanish.

## SECTION 5 — INSTRUMENTS THAT LIED THIS SESSION

- ⛔ **`terminal_session_keys` is OWNED + PRESERVED.** Use
  `owned_terminal_session_keys`. The old note attributed PTYs to whichever daemon
  merely held a bridge record.
- ⛔ **A process sweep must anchor on `argv[0]`** — read `/proc/<pid>/cmdline`,
  take the first NUL-separated field, require its basename to be the binary. The
  rule failed three times in one session in weaker forms: `pgrep -cf` counts your
  own shell; `grep -v grep` does not exclude the shell; matching `claude` against
  the whole cmdline matches every shell, because the snapshot path contains it.
- ⛔ **`~/.yggterm` trace files are shared with `cargo test`.** A corpus-wide
  count returned exactly the fixed behaviour — from a test binary. Filter by pid
  and confirm each pid is a daemon.
- ⛔ **`strings <binary> | grep <a json! KEY>` can read absent on a correct
  build.** Pick a literal that appears as a VALUE, cross-check with `nm`.
- ⛔ **`server daemons`: count VERSION rows, and allow for the `*`** on the
  serving daemon, which shifts every field by one. Indented `deferring:` lines
  interleave and vary with activity.
- ⛔ **"No rows died" cannot be established by watching a fixed interval.** I
  fitted a window to the previous maxima and missed a death at 464 s. Census by
  uuid, before and after, re-checked at the next relay boundary.
- ⛔ **A `docs-ssot` status line must be EXACTLY one of the three strings.**

## SECTION 6 — HOLDS AND PROHIBITIONS, ALL LIVE

- ⛔⛔ **3.0.155 IS HELD.** A daemon bump TYPES: the hot-restart repair submits
  `continue` through the very probe that types over a draft. The queue entry's
  ordering clause forbids bumping while a human holds an unsent draft. **I broke
  this rule** — I honoured "do not relaunch the GUI" and deployed the daemon,
  which is the MORE dangerous of the two, then read the entry afterwards. No harm
  (no interrupted record, 0 `repair_continue`, and no dev daemon owns that row),
  but the check was made in the wrong order.
  ⇒ **The rule to carry: do not bump while a draft is open unless the outgoing
  daemons can be shown to converge gracefully.** The `continue` rides the
  **forced** shutdown path; a graceful handover records nothing and submits
  nothing.
- ⛔ **Never relaunch the owner's GUI**, and never `input-check` row 9.4 — the
  probe types first.
- ⛔ **Any git verb that RESTORES A PATH is the hazard.** Commit `wip(...)` often.
- ⛔ **Do not resolve another lane's conflict hunks**; ask for it rebased.
- ⭐ When it lifts, 3.0.155 should carry `lane/dev/6.6-arsenal` and 6.7's S2
  daemon-side work, which was **not** in 3.0.154 (verified by symbol).

## SECTION 7 — THE DISCIPLINE THAT ACTUALLY PAID

- **Falsify your own test before trusting it.** Every fix here was run against a
  mutant that disabled it, and each failed with the original symptom first.
- **Both controls in the same run.** The pty-thread proof and the persist-drop
  proof both *passed* — and proved nothing, because the negative control passed too.
- **A finding is not a mechanism.** Three deaths with a matching signature is
  still coincidence in time until an exit path is traced.
- **Read the artefact, not the exit code.** A fabricated uuid reached a ledger
  whose entire value is being trusted, and only the read-back caught it.
- ⭐ **The instrument's silence can be STRUCTURAL rather than evidential** — a
  crossing that checks one direction reports "0 warnings" over the hole it cannot
  see. Pair that with "a control that also passes supports nothing".
