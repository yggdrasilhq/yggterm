# Brief for the next 6.1 — the hot-restart relay gate

Seat `6.1`, host `dev`, cwd `/home/pi/gh/yggterm`. Orchestrator: row `6.0`.
Rewritten 2026-08-13 ~20:30 IST by session `4552bad4`.

## SECTION 0 — FIRST ACTS

1. `~/gh/yggterm/.agents/skills/yggterm-agent-fleet/ygg-claim.sh --host *** --title "yggterm: the hot-restart relay gate — the constitution's unmet guarantee" --number 6.1 --replace 4552bad4-7d5b-4378-bc23-295b3cbbf0f8 --booter`
   ⚠ `--host ***` is still required; auto-detect of the GUI host fails from `dev`.
2. `ygg-monitor.py subscribe --uuid <yours> --role relay --seat 6.1 --machine dev --escalate-to a516fe06-594c-4d82-9c70-b8d5095b7f44 --intent "hot-restart relay gate"`
3. Read the queue entry `## ⚖⚖ THE HOT-RESTART GATE IS UNBUILT` in
   `docs/pending-bugs.md`. It is current and carries every falsifier. The spec is
   `docs/spec-hot-restart-relay-gate.md`.

## SECTION 1 — WHAT IS DONE, AND WHAT IT IS PROVEN BY

**§4 — closed, live-proven on both producers.** The GUI producer was closed
today: `startup_hot_swap_declined_swap_queued` on the GUI host, then two
`unchanged` repeats with `requested_at_ms` NOT moving, which is the half that
matters because §5's deadline is measured from that stamp.

**§2 — LIVE-PROVEN (3.0.130).** Real daemon, real retire loop, real queue entry
that could not converge, both floors engaged: 100 s pinned at `attempts: 1`,
boundary declared at T, `attempts: 2` at **T+13.4 s** with
`last_outcome: "a relay boundary was declared; retrying the handoff at it"`,
then holding at 2 — one boundary buys exactly one attempt.

**§5's repair half — LIVE-PROVEN (3.0.130), down to the PTY.** `repair owed:` on
`server daemons`, `hot_restart_repair_continue {outcome: "submitted"}`, and the
session's own stream showing probe → echo → `^U` → `continue` → a lone `\r`,
answered by the shell complaining that `continue` is only meaningful in a loop.
That complaint is the proof it arrived as a SUBMITTED LINE.

**§3's prerequisite — BUILT AND LIVE-PROVEN (3.0.132): `server gate-screen`.**
31 of 31 sessions readable on a live fleet daemon with real screen text in every
tail, against 205-of-225-were-a-summary for the old instrument. The corpus §3
needed now exists.

**The socket bequest — BUILT AND SHIPPED (3.0.132), NOT PROVEN.** See §3 below.

## SECTION 2 — YOUR SCOPE, IN ORDER

1. ⭐ **Fix 1 of the socket-alias entry: a caller that cannot reach its pinned
   socket falls back to the CURRENT daemon.** It is ranked PRIMARY in that entry
   and it deletes the whole class; everything else there is mitigation. It is
   also the durable form of the outage that reached the owner tonight as *"I
   cannot type in many sessions"*. ⚠ **"Same host, same home" must be CHECKED,
   not assumed** — the entry says why, and a fallback that succeeds wrongly is
   worse than the honest error.
2. **Watch the bequest at the next handover** (§3 below). One observation.
3. **Blocked-on-human**, now that `server gate-screen` can harvest a real corpus.
   ⛔ Harvest into the scratchpad, never into the repo: those are the owner's
   sessions.
4. The positive WORKING signal (`session_transcript_activity`) — source proven,
   transport not.
5. ⚠ **Housekeeping owed and deliberately not rushed: the project's memory index
   is 23.6 KB against a 24.4 KB read limit**, and its own hook asks for under
   17 KB. Most long lines are pure door-lists that cannot shrink without losing
   a door, so this needs a deliberate pass that moves prose into topic files —
   not a trim at the end of a long session. Do it FIRST, while you have room:
   an index that stops loading costs every future session on this project.

## SECTION 3 — THE ONE THING YOU MUST CHECK BEFORE TRUSTING WHAT I SHIPPED

`alias_own_socket_to_successor` (3.0.132) makes a retiring daemon symlink its own
version socket at its successor's, on both handover exits. **It reaches the next
generation only**, so its first real firing is the 3.0.132 → 3.0.133 handover.

⚠ **And a hand-made alias at the just-retired version was deleted three times
within seconds-to-100 s of the retirement**, while aliases at three other
versions created in the same command survived, and while the same alias
recreated seven minutes later survived 150 s. The deleter is transient, fires in
a window around a retirement, and targets exactly the name the bequest writes.
**Mechanism is a HYPOTHESIS** (a sweep condemning a name on a dead sighting taken
while it was still a dead real socket, executed later against a live alias) and
is written up in the queue entry. ⇒ **Falsifier: at the next bump, watch
`server-3-0-<retiring>.sock` for two minutes.** If it disappears, my fix is inert
until the sweeper re-checks liveness at the moment of unlink.

## SECTION 4 — WHAT WILL SAVE YOU TIME

- ⛔ **§5's Force half cannot be manufactured headlessly, and must not be forced
  on a live host.** It needs an all-interruptible blocker set, i.e. an agent-kind
  session with no plain shell beside it. `server attach` mints a shell (exempt —
  it vetoes the force and hides the state under test); `automation run` routes
  through app-control and refuses where there is no GUI. The two honest routes
  are catching it in the wild, or a full GUI inside
  `scripts/underglass-sandbox.sh`'s private sway + private `YGGTERM_HOME`.
- ⭐ **A sandbox daemon is cheap and answers most of these questions.** Copy the
  release binary, `YGGTERM_HOME=<scratch>`, `server daemon`, then
  `server attach <name> /tmp --allow-plain-shell-fallback` for an owned session.
  To make it owe a swap: `mv` a wrapper over its binary that reports a newer
  `--version` and exits on `server daemon` — the "successor that never arrives".
  `YGGTERM_DISABLE_SELF_RETIRE_HANDOFF=1` sends it to the cold gate instead.
- ⛔ **A daemon FREEZES its launch environment and every session it later spawns
  inherits it.** Never start one from an ssh shell. Read `/proc/<gui>/environ`
  and launch with that. This is in the queue entry as part of the repair recipe.
- ⚠ **The deadline's clock only starts where the swap lane gives up.** With a
  queue entry present the drainer returns `Lingering` and the cold gate — and so
  §5 — is never evaluated. Its real trigger is `newer_daemon_live`, empty hands,
  or the handoff kill-switch.
- ⚠ **Adding a `ServerRequest`/`ServerResponse` variant fails
  `protocol_shape_stamp_forces_version_bump`.** That is the test doing its job:
  allocate a version with `scripts/bump-version.sh` (it pushes the bump ALONE and
  FIRST), then update `STAMPED_AT_VERSION` + `STAMPED_SHAPE_HASH` — the failure
  message prints the hash to paste.
- ⚠ **`role_gate` is an exhaustive match with no wildcard**, so a new variant
  will not compile until you classify its shadow access. Classify by SCOPE, not
  by whether it mutates.
- ⚠ The rare pre-existing flake
  `tests::local_cc_relaunch_rebuild_collapses_poisoned_identity_to_row_id` did
  not appear today across several full runs. If you see it, reproduce on a clean
  tree first.

## SECTION 5 — THE STATE YOU ARE BORN INTO

- The fleet is at **3.0.132** (commit `e1ec036f`), all four copies on all three
  hosts. The push hold is long lifted.
- **The GUI host converged today from twelve versions behind**, by starting a
  coexisting daemon at the on-disk version with the GUI's own environ. The
  superseded daemon handed over 7 PTYs alive and retired on its own terms; the
  ancient lingerers were untouched. ⇒ **A second daemon does not disturb a
  pending hot-restart handshake — it completes it.**
- ⛔ **Never `git push --tags` / `--follow-tags`.**
- ⛔ Before believing a `ygg-privacy-guard` refusal, check
  `git rev-list --left-right --count origin/main...HEAD`. Thousands on the left
  means the BRANCH is the problem, not your commit.
- ⭐ The law worth carrying, and it earned its keep twice today: *am I choosing
  between explanations, and does one of them happen to authorise what I was
  already about to do?* Its sibling, from the peer traffic tonight: **a brief may
  carry measurements; it must not carry a causal theory.** Both times the
  suspected gate turned out to be a bystander.
