# 6.3 — the deaf row, the sidebar, and the rail that draws nothing

ACK-6POINT3-CHROMEQUERY — quote this token back in your first reply.

Seat `6.3`, host `dev`, worktree `~/gh/yggterm--ux`, branch `lane/dev/6.3-ux`.
Orchestrator: seat 6.0, row `444e8ff2-2a2e-4440-9d21-ee51b01af29e`.
⛔ Claim your row FIRST: `.agents/skills/yggterm-agent-fleet/ygg-claim.sh
--host jojo --title "chrome query: read the DOM without typing into it"
--number 6.3 --replace <the row that spawned you>`.

## ⭐ YOUR JOB, IN ONE LINE

**Build the read-only DOM query for the app's own chrome**, then use it to
settle why the right rail draws nothing. 6.0 sanctioned it explicitly under
CLAUDE.md's *"extend yggui rather than hand the task back"*. It is the
prerequisite for the rail, and it outlives the rail.

### What it must do

- A verb that **reads** the shell's own DOM: given a selector, answer whether
  the node exists, its **child count**, and its **computed geometry**
  (`getBoundingClientRect`) — and nothing else.
- ⛔ **Read-only means read-only.** No synthetic input, no focus change, no
  click, no navigation. The reason the existing affordance is useless is that
  `server app chrome type --assert <selector>@<attribute>` is the only DOM read
  in the surface and it **types first**, which may not be pointed at a live GUI
  the owner is using.
- Model it on the existing `server app chrome` dispatch. ⚠ `server app` is
  dispatched in TWO files — grep for the sibling verb and change both, or the
  verb exists and answers nowhere.

### The falsifier it exists to run

Point it at the rail's content div (`[data-yggui-side-rail-content]`):

| reading | verdict |
|---|---|
| children **absent** | the body never rendered — fault is UPSTREAM of the styles |
| children present, **zero size** | it is layout |

⚠ **Point it at something whose answer you already know first** — the LEFT tree,
which is measured to draw (0.79 % ink). An instrument that has collapsed to a
constant answers your real question confidently and wrongly, and this campaign
has been burned by exactly that.

## STATE — what is landed

| item | state | where |
|---|---|---|
| wedge signal → sidebar (session rows) | **LANDED, not live-proven** | `2edb2e54`, by seat 6.7 |
| collapsed group hides a deaf row | **LANDED, not live-proven** | `a6eaf7f8` |
| the right rail draws no body | **FILED, cause not established** | `627f605e` |
| live amber-dot proof on the GUI host | ⛔ **NOT DONE** | — |
| is 12 s the right threshold | ⛔ **NOT MEASURED** | — |

All pushed to `origin/main`. 0 unpushed at handover.

## ⛔ READ THIS BEFORE RE-DOING ANY OF IT

**The wedge→sidebar wiring already exists. Do not build it again.** Two seats
built it in parallel on one evening because a routing message and a landing
crossed. 6.7's version is the one on `main`; mine was discarded unbuilt. The
threshold has ONE owner — `INPUT_UNANSWERED_WEDGE_SUSPECT_MS` in
`yggterm-core` — and gate, row payload and sidebar all read it.

⚠ **`git merge-base --is-ancestor <sha> origin/main` before acting on any sha
quoted anywhere.** It is what turned "two seats disagree" into "the state moved
fifteen minutes after the measurement was taken" — nobody was wrong, the
difference was TIME.

## THE TWO ITEMS THAT ARE ACTUALLY OPEN

### 1 + 3 — they share one apparatus, do them together

To see the amber dot you must manufacture a genuinely deaf row; that same row
is the only thing that can answer whether 12 s is right. You will not get one
cheaply twice.

- The reproducer that is known to work: `kill -STOP` the CLI process of a row
  **you created** (`--purpose`, `--ephemeral`). Alive, composer drawn,
  consuming nothing — the wedge's exact signature.
- The gap is `last_activity - last_output`, and `last_activity` is stamped by
  the WRITER. So you must **write to the row** after STOPping it, or the gap
  never opens.
- ⚠ A healthy child echoes in milliseconds — even a keystroke it means to
  ignore. So the false positive to measure against is a child with **echo
  off** (a password prompt), not a slow one.
- Read it back with `server app rows`, which emits `input_unanswered_ms` and
  `wedge_suspected`.
- `kill -CONT` and remove the probe row afterwards
  (`server app session remove`, GUI host only).

⛔ The GUI host is sequenced by 6.0. Ask before deploying or cycling; a
measurement window must never straddle a deploy.

✅ **THE DEPLOY IS DONE** — every copy on every host reads `98b6c6c54380`.
⭐ `scripts/deploy-fleet.sh --preflight` FIRST, before paying for the build: it
refused on the first ask because main had advanced mid-session, and that is the
failure it exists to prevent.

⛔⛔ **AND THE PROOF IS STILL BLOCKED, ON HIM, NOT ON WORK.** The sidebar is
drawn by the GUI PROCESS, which keeps its old rendering until it relaunches —
so a deployed binary changes nothing on screen. A relaunch discards the unsent
half-typed draft he is holding in a live composer. Filed in
`docs/owner-attention.md`. **Do not relaunch to get your screenshot.** The
constitution makes a restart free; an unsent draft is the case where it is not.

### 2 — the right rail (`627f605e`), blocked on a missing instrument

The entry carries the measurements. The one thing that would settle it does not
exist: **there is no read-only DOM query in the app-control surface.**
`server app chrome` offers only `type`, and its `--assert <selector>@<attribute>`
is the sole DOM read but types first, so it cannot be pointed at a live GUI.

⇒ **Build the read-only chrome query.** That is the prerequisite, and it is
worth more than this one bug: every future "the chrome renders wrong" question
needs it.

⛔ **Two candidates are already EXCLUDED — do not re-walk them.**

- **The docked-rail style-key ghost** (`spec-sidebar-auto-hide-hover-overlay.md`).
  The obvious suspect, because this symptom is a verbatim recurrence of the one
  that trap first produced. It does not hold: both geometry helpers build ONE
  `format!` with a fixed key set and vary only values — the very fix that trap
  was closed with.
- **A collapsed `zoom`.** The tree and the rail pass byte-identical
  `zoom_percent_f32(settings.ui_font_size, 14.0)` to the same helper, and the
  tree draws.

⚠ **Correct one thing I said early and wrongly:** the pale band is not the card
painting. In-flow the card is `background-color:transparent` BY DESIGN and the
app background paints behind it. The measurements stand; "the card paints and
the body does not" was the wrong mechanism for them.

## METHOD NOTES THAT COST SOMETHING TO LEARN

- ⛔ **A revert harness must not use git.** `git checkout -- <file>` reverted
  ~120 lines of uncommitted work, not just the stub it was meant to undo, and
  `git status` then looked clean. `cp` the file aside instead, and commit a
  `wip` checkpoint before running anything that writes to your source.
- ⭐ **Exclude the confound before the diagnosis.** The window is composited
  transparent, so "that band is wallpaper showing through" would have explained
  the whole symptom. Ink fraction settled it: wallpaper has no near-black text
  pixels, the left tree has 0.79 %, the rail has 0.00 %.
- ⭐ **A privacy constraint and a faithful pixel are compatible.** Capture the
  full frame on the GUI host, crop it THERE, and fetch only the crop — the
  frame with the owner's session in it never leaves his machine, and
  `capture_faithful` still applies. Numeric probes (`ink_fraction`,
  `edge_energy`, one scanline of hex) answer "is anything drawn" without
  looking at content at all.
- ⚠ **`submitted: false` is a refusal, not unreachability.** A busy row will not
  echo, so `terminal submit` hammers it for the full timeout. Two tries, then
  use the peer plane. (Root cause now filed by 6.7: the readiness probe types
  over the human.)

## ⛔ HOUSEKEEPING THAT HAS BITTEN THIS SEAT

- **Work in `~/gh/yggterm--ux` only.** `~/gh/yggterm` is a SHARED checkout and
  somebody's desk. ⛔ No `reset --hard`, no `checkout -- .`, no `clean -fd`
  there — a peer lost ~1200 lines to one such command tonight, unrecoverably.
- ⛔ **And the danger is any git verb that RESTORES A PATH, not the shared tree
  specifically.** `git checkout -- <file>` inside your own worktree reverts the
  WHOLE file, not just the edit you meant to undo; it ate ~120 lines here and
  `git status` then looked clean. A mutation harness must `cp` the file aside
  and `mv` it back. Commit `wip(...)` checkpoints every few edits.
- **MERGE, never rebase.** `docs/pending-bugs.md` is semantically ordered and
  N-against-M rebase conflicts drop other lanes' entries. Where two entries are
  independent additions, KEEP BOTH SIDES.
- ⛔ **`git merge-base --is-ancestor <sha> origin/main` before acting on any sha
  quoted in any doc, memory or message.** The history rewrite orphaned every
  pre-rewrite hash and the objects still resolve locally, so a dead sha looks
  alive. It is also what turned "two seats disagree" into "the state moved
  after the measurement" — nobody wrong, the difference was TIME.
- **Land queue edits on `origin/main`**, not only on the lane branch. Remove an
  entry in the same commit as its verified fix. `scripts/check-docs-ssot.sh`
  is enforced — a `**Status:**` line takes the bare status and nothing else.
- `~/gh` is **public GitHub**: no host names, no personal paths, no real row
  titles, no attributed speech. Invent every example.
- ⭐ **`scripts/deploy-fleet.sh --preflight` BEFORE you pay for a release
  build.** It refused here on the first ask because main advanced mid-session;
  asked after a build, that costs the whole build.

## ⛔ THE GUI HOST IS NOT YOURS RIGHT NOW

Seat 6.7 holds jojo for RAM measurements; 6.0 sequences it. Ask 6.0 before
deploying or cycling — a measurement window must never straddle a deploy.

⛔⛔ **DO NOT RELAUNCH THE GUI TO GET A SCREENSHOT.** The owner holds an unsent
half-typed draft in a live composer. The constitution makes a restart free; an
unsent draft is the one case where it is not. That is an owner gate, filed in
`docs/owner-attention.md`, and it is what the amber-dot proof waits on — not
work. If your own work needs a relaunch, STOP and tell 6.0.

⭐ **You can still take a faithful pixel without exposing him:** capture the
full frame ON the GUI host, crop it THERE (`magick ... -crop`), and fetch only
the crop — the frame with his session in it never leaves his machine, and
`capture_faithful` still applies. Numeric probes (a scanline of hex, ink
fraction, edge energy) answer "is anything drawn" without looking at content at
all. That is how the rail was measured without reading a line of his terminal.
