# 6.6 ARSENAL — relay brief

You are seat **6.6, the ARSENAL lane**. Host `dev`, cwd `~/gh/yggterm--arsenal`,
branch `lane/dev/6.6-arsenal`. It is YOUR worktree: keep it, and never `git reset --hard`
or `git checkout --` a file you have uncommitted work in (a shared checkout).

Orchestrator: seat **6.0**, row `remote-cc://dev/294d0d3c-3fe0-4504-bf96-a70f123f0331`,
uuid in full `459e6b63-f6ed-4118-a994-564efdfc2dc1` for monitor/escalation fields.
⛔ **WRITE FULL UUIDs, NEVER THE EIGHT THE BOARD PRINTS.** A short `escalate_to` broke
routing, repair and the board at once; it was fixed and then reproduced within the hour
by someone quoting eight characters.

## STATE — the lane is GREEN and nothing is blocked

Lane tip when this brief was written: `923698b6` (plus this brief's own commit).
`git fetch origin main` and merge before you start; main moves fast (224 commits landed
during the previous session).

⭐ **The order that matters: merge → test → push.** I pushed a merge before testing once
and briefly carried someone else's red on my lane.

Verification commands this lane uses:
```sh
cargo test --workspace -- --test-threads=1     # serial; see the parallel note below
bash scripts/check-docs-ssot.sh                # ALWAYS its own call before a push, never chained
git push origin lane/dev/6.6-arsenal           # the privacy guard runs pre-push
```

## THE TWO ITEMS ROUTED TO YOU — ⛔ BOTH DIAGNOSES BELOW WERE WRONG, CORRECTED 2026-08-14

⚠ **Read this section as history, then read the queue.** The two summaries below are kept
because they say what this lane believed on arrival, and the corrections underneath are the
lane's own most useful output. Everything measured lives in `docs/pending-bugs.md` and
`CHANGELOG.md`; those own the state, this does not.

### 1. "A local launch into a cwd that exists only elsewhere leaves a HUSK row"
**Believed:** a keep-alive row that *never had a process*, plus a detector already present
and swallowed at the CC title poll's `local_cc_session_jsonl_path` lookup. **The owed work
was said to be: validate the cwd at launch, and un-swallow the detector.**

⛔ **Both halves were wrong, and reproducing it with a control is what showed it.**
- The launch **succeeds**. `best_effort_cwd_shell_prefix` walks up to the nearest existing
  ancestor and falls back to `$HOME`, deliberately; `claude --session-id <the row's own
  uuid>` was found running, in `$HOME`. No transcript existed only because CC writes one on
  its first turn. ⇒ **"validate the cwd at launch" would have refused a launch the product
  intentionally allows.** The real defect was that the row went on describing itself as
  *rooted at* a directory the process had never been in.
- The "swallowed detector" sits **behind a working-set gate**, so it was unreachable for a
  row that never ran. Two different faults shared one comment. The signal it names is real
  and now has its own home (`collect_stuck_launch_faults`), keyed on **mid-turn** rather
  than the working set — the looser key fires on every healthy launch before its first turn.
- ✅ **Shipped and live-verified against a control.** Entry removed from the queue.

### 2. "Real-key input delivers nothing on the GUI host — the defect is live in current main"
**Believed:** the stale-binary hypothesis was refuted by grep, therefore the defect was live
in `main` and should be diagnosed as code.

⛔ **The refutation was sound; the inference from it was not.**
- ⚠ The grep that refuted staleness had scanned `crates/` — and **the mechanism is not in
  `crates/`**, it is in `vendor/dioxus-desktop/src/web_surface.rs`. Widened, the refutation
  still holds, but only because it was re-run. *An absence in a tree the symbol was never in
  is not evidence.*
- **Six controlled arms say the mechanism is healthy**: both builds, both machines, and the
  mapped / hidden / born-headless surface states, each with `do click` as the control in the
  same run. `scripts/web-real-keys-ab.sh` is the landed harness — ⛔ **run it, do not
  re-derive the arms.**
- The GUI that produced the failures has since been replaced (verified by exe md5, not by
  version). ⇒ **the reproduction is gone**; the entry is *open, not reproducible*, and it is
  deliberately NOT an owner gate.
- ⭐ Still true and still the reason it mattered: `delivered:false` was the verb being
  HONEST, and that was luck. Every credential path on that host routes through real keys, so
  a caller that ignores `delivered` types nothing into a payment field and reports success.

## ALSO OPEN, AND MINE — do not lose these

- **The `set_var` product race** (`sync_claude_extra_args_for_request`, process-wide env
  write read back by the launch composer). ⛔ **FROZEN by 6.0's instruction** — the CC
  lane's designed round-trip, not to be changed under the release hold. ⚠ A test guard
  (`env_test_guard`) made the parallel suite green; that silenced the HARNESS, not the
  product. The entry has the split; do not let a green run close it.
- **Grok title field** — open for ONE organic observation: a grok session long enough that
  it generates a title. ⛔ Do not spend the owner's xAI quota to force it. The reader now
  prefers `generated_title` over `session_summary` on the strength of grok's own schema
  documentation, and the entry says that is not yet a sighting.
- **Four `server` verbs** still answer from one binary only. `connect` has a **verdict**
  (accidental — no window, no app-control, no spawn) but its move drags eight dependencies
  (~190 lines); I judged it bounded, it was not, and I reverted rather than half-land it.
  `gate-screen`, `relay-boundary`, `wpe` read as genuine headless-only forks — that is a
  reading, not a verdict.

## THE LANE'S LAWS — these cost real time to learn

- ⭐ **TABLE or FORK.** When a `match` arm or a per-CLI value looks per-binary, ask whether
  the TABLE forks or a single CELL does. `server app` was one table with one forked cell
  (launch) ⇒ full collapse. `server` mixes planes ⇒ per-verb, and a structural ban there
  would forbid a real fork.
- ⛔ **A fork can be replaced by a TABLE or by a GLOBAL, and only one is a fix.** The
  2026-08-13 intake did both in one function: slug-keyed settings (right) and one
  kind-blind env var for nine CLIs (wrong, and live in running daemons).
- ⭐ **Grep for the SETTER before believing an env read is a channel.** No setter anywhere
  ⇒ it is not a channel, it is inherited pollution.
- ⛔ **A source-scanning lock matches PROSE.** Three times in one session a lock matched its
  own comment or its own assertion string. Scan the product half
  (`source.split("mod tests {").next()`), strip `//` lines, and bound arms at their own
  close brace — indentation-sensitive bounds silently re-aim onto the next arm instead of
  failing.
- ⛔ **Two tests failing together are a claim about ONE cause. Read the second before
  editing the first.** The cheapest one to rationalise is the one that misleads you: one
  looked like a stale enum name and was, in fact, reporting a live regression that the
  second test made obvious.
- ⭐ **A number equal to a constant in the suspect path is the mechanism signing its own
  work** (`waited_ms: 180` == `PROBE_SETTLE`). Hunt for that before believing a story.
- ⛔ **Validate a source scanner against a case whose answer you already know.** My first
  verb extractor omitted `daemons` entirely because that verb uses a different dispatch
  spelling; only a live observation caught it.
- ⛔ **`~/gh` is PUBLIC.** Invent every example. The privacy guard refused a push of mine
  over an invented UUID ending in twelve digits — rewrite, never `YGG_PRIVACY_ALLOW=1`.
- ⛔ **Never end a turn on a question.** Owner-gated ⇒ `docs/owner-attention.md` with your
  recommendation and what you did meanwhile, then carry on.

## MESSAGING OTHER ROWS

`input-check` first, then `submit`, then **verify by grepping the target's transcript for a
token from your own message**. ⛔ `submitted:false` after ~30 s means the row is mid-turn —
that is correct behaviour, NOT a failure. **Never retry it.** I once re-ran a submit merely
to re-parse output I already had and delivered the same report twice.

```sh
ssh -n jojo "~/.yggterm/bin/yggterm server app terminal input-check <row> --check-timeout-ms 10000"
ssh -n jojo "~/.yggterm/bin/yggterm server app terminal submit <row> --stdin < msg.md"
```
