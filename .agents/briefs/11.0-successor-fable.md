# 11.0 SUCCESSOR — cli-integration orchestrator — ACK-110-RELAY-9d3c

You are the successor to seat **11.0**, orchestrator of the 11.x cli-integration wave.
Owner-ordered onto **Fable 5**, spawned with `--model claude-fable-5` so the model is set at
spawn and never switched in-session.

## FIRST ACTS, in order

1. **Verify your model and say it plainly.** `grep -o '"model":"[^"]*"' <your transcript>`.
   ⛔ Never run `/model` on a warm session — a conversation is prompt-cached against ONE model and
   a switch re-reads the whole accumulated history. Model is set at spawn or not at all.
2. **READ THE DOOR IN FULL** — `~/.claude/projects/-home-pi-gh-yggterm/memory/
   campaign-11x-cli-integration.md`. Start at **§THE DAEMON ADOPTION GAP** and the
   **§11.0 SESSION 2026-08-20 14:06** block; everything above them is history that corrects itself.
   Then skim the fleet SKILL.md §1 §3 §7 §8 §10.
3. **KILL YOUR PREDECESSOR** (skill §8): row `remote-cc://dev/df5f87ea-159e-41ab-90a7-6d5c68d3b1ef`.
   ```
   .agents/skills/yggterm-agent-fleet/ygg-claim.sh \
     --title "cli-integration: orchestrator — all-CLI attach/persist/title + ytrace render profiling" \
     --campaign cli-integration --replace df5f87ea-159e-41ab-90a7-6d5c68d3b1ef --inherit-number
   ```
   Then PROVE the reap independently — the tool's read-back does not count: no process carries the
   uuid AND the row is absent from `server app rows` **run on the GUI host**. ⛔ A row that remains
   with `presence: "row"` / `live_member: false` is the DURABLE on-disk session, which is correct
   and must not be removed — that is the product working, not a husk.
4. **Re-arm the watch.** Copy `~/.yggterm/relay/spawned-by-df5f87ea-*.txt` to your own uuid, drop
   any reaped lane, subscribe yourself to the monitor (`--escalate-to ""`) and the booter, and
   re-defer the booter each monitoring turn. ⭐ **Check `ygg-monitor.py list` prints
   `✅ watcher pid=`** — it was found not running at all earlier today while the plane reported
   itself healthy. The current watcher is detached 24h from 14:01 and WILL expire on you.

## STATE — carried here, not left to inference

- **11.1 [scan-truth] is LIVE on Fable**, `local://52f67c2e-35ff-4db7-ba8e-168bdbf567b4`, working in
  the GUI host's checkout. Brief: `.agents/briefs/11.1-gui-count-ssot.md`. It is editing
  startpage/cwdtree/titles in BOTH that checkout and this one — do not clobber; rebase with
  autostash and commit only your own files.
- **12.x is a sibling campaign, alive and kickstarted**: 12.0 (266084ed) with delegates 12.3
  (66c1b3bc) and 12.4 (c44a7c14). ⛔ 12.0's ROW is a frozen view over a live agent — it has been
  told to relay-hand-over. Not yours to reap.
- **All other 11.x lanes are landed and reaped** (11.1-old, 11.2, 11.3, 11.4, 11.5, 11.6, 11.7).
- **11.5 render lane is NOT respawned.** Brief armed at `.agents/briefs/11.5-render-relay.md`; its
  own reasoning (everything gated on the lock-scope deploy soaking) still holds — re-read it before
  deciding, and either spawn it or drop it rather than leaving it half-armed.

## OPEN, in the order I would take them

1. **Finish/verify the daemon adoption on dev and oc** if it is not already green when you land.
   3.1.7 was bumped and deployed for exactly this. ⛔ **Verify against the LIVE pid** —
   `readlink /proc/<pid>/exe` must not end "(deleted)", and the start time must be after the deploy.
   The on-disk binary proves the WRITE, never the ADOPTION.
2. **The apt/DECSTBM client-paint half.** Parse side is cleared (two independent emulators agree);
   what is owed is a FAITHFUL capture of a plain-shell row, which needs the row visible — take it
   only when the owner's viewport is free. `scripts/decstbm-probe.sh` is the instrument.
3. **11.1's row shows the START PAGE instead of its session** (`State: No session selected`) while
   the daemon reports that session working. Owner-reported, not root-caused. ⛔ Do not assume it
   shares 12.0's orphaned-PTY cause without deriving it.
4. ⛔ **LAST, owner-ordered:** the N.x.y resurrection (door §PARKED FINAL STEP).

## STANDING LAWS

His viewport is sacred: never activate his row, never type into a live prompt, and ⛔ never run
`input-check` while he is at the machine — it WRITES a marker; `server gate-screen` is the read
door. Every claim names its instrument. `--machine-key` is where the WORK lives; `--machine` is not
a flag and lands the row LOCAL without complaint. A `--cwd` absent on the target does not fail — it
walks UP a directory; `ssh <target> test -d <cwd> ||` refuse. Verify a brief landed by grepping the
delegate's transcript for a token from your own brief, never by the create reply's `prompt` block.
Push every repo; `git fetch` before believing any behind/ahead count, and remember a LANE branch's
commits can be safely pushed on their own ref while still reading `ahead` against origin/main.
⛔ `~/gh` is PUBLIC: no host names, no personal paths, no attributed speech, invent every example —
the pre-push guard refuses, and the answer is to REWRITE, never `YGG_PRIVACY_ALLOW=1`.
