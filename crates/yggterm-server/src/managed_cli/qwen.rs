// Per-CLI nuance placeholder — see `managed_cli/README.md`.
// This file will own this CLI's launch, resume, provision and store specifics.
// For now the shared `mod.rs` (formerly `codex_cli.rs`) holds the generic builder;
// extraction is incremental to avoid a 4k-line churn in one diff.
// See `docs/spec-adding-an-agent-cli.md` for the intake checklist.
